import React from 'React';


// stateless functional component -> Recieves props as its first argument, if it doesnt have state.
const RemoveItem = () => {
	return (
		<button className="header">Remove Item</button>
	);
};

export default RemoveItem;
